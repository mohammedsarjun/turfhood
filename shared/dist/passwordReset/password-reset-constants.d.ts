/** Default password-reset token validity window in seconds — the single source of truth shared by frontend and backend defaults. */
export declare const DEFAULT_PASSWORD_RESET_EXPIRY_SECONDS = 1800;
